{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "python -m pip install -r requirements-vercel.txt",
  "functions": {
    "app.py": {
      "excludeFiles": "{.venv/**,tests/**,frontend/**,docs/**,data/**,models/**,reports/**,src/training/**,src/evaluation/**,*.pdf,**/*.pdf}"
    }
  }
}
